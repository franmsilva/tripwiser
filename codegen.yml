overwrite: true
schema: "http://localhost:4001/"
documents: null
generates:
  src/types.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
  ./graphql.schema.json:
    plugins:
      - "introspection"
